import { createApp } from 'vue'
import App from './App.vue'

const app = createApp(App)
app.mount('#app')

// const root = document.getElementById('app')

// root.textContent = 'hello webpack5 and vue3'
